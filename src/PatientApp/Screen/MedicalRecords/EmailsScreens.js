import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const EmailsScreens = () => {
    return (
        <View>
            <Text>EmailsScreens</Text>
        </View>
    )
}

export default EmailsScreens

const styles = StyleSheet.create({})
